<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZapStation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Personaliza los colores aquí -->
        <item name="colorPrimary">#083ECC</item> <!-- Usa el color que definiste -->
        <item name="colorPrimaryDark">#083ECC</item>
    </style>

    <!-- Custom firebase UI -->
    <style name="FirebaseUITema" parent="FirebaseUI">
        <item name="windowNoTitle">false</item>
        <item name="windowActionBar">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="colorPrimary">#083ECC</item> <!-- Color personalizado para Firebase UI -->
        <item name="colorPrimaryDark">#388E3C</item>
        <item name="colorControlNormal">#000000</item>
        <item name="colorControlActivated">#000000</item>
        <item name="android:windowBackground">@drawable/background_semi_transparente</item>
    </style>

    <style name="Theme.ZapStation" parent="Base.Theme.ZapStation"/>
</resources>
